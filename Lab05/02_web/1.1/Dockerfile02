FROM nginx:stable

ENV TZ=America/Lima
#ADD ./dist /usr/share/nginx/html

ADD /dist.tgz /dist.tgz
RUN ls -lt /dist.tgz/dist && \
    cp /dist.tgz/dist/* /usr/share/nginx/html && \
    ls -lt /usr/share/nginx/html

ENTRYPOINT ["nginx", "-g", "daemon off;"]